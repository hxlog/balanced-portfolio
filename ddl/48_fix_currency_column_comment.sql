-- =====================================================================
-- 48: 修正 bp_index_config.currency 的列注释(口径已变, 注释仍是旧的)
--
-- 背景: 该注释是 40 号迁移写的, 当时「非 CNY 资产无汇率可用、不进回测面板」是事实;
-- 43-45 号接入 fx_sina 后口径已变为「清洗阶段按每日汇率折算成人民币再进面板」,
-- 但列注释没跟着改, 于是 `\d+ bp_index_config` 与任何读 pg_description 的工具
-- (含部分 ORM/文档生成) 都在向运维陈述一个已被推翻的事实。
--
-- 只改注释, 不动任何数据与结构。COMMENT ON 天然幂等(重复执行结果一致),
-- 生产与全新环境执行后状态相同。
-- =====================================================================

COMMENT ON COLUMN bp_index_config.currency IS
  '资产计价币种。CNY=人民币计价; 非CNY(USD/HKD/JPY等)=外币计价, 清洗阶段按每日汇率(fx_rate 列)折算成人民币后进回测面板。builder 把非 CNY 沉底并在添加前二次确认。';
